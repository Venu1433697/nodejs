version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing project dependencies...
      - npm install

  build:
    commands:
      - echo Running SonarQube Analysis...

artifacts:
  files:
    - '**/*'
